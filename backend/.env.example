# Backend Environment Variables
# Copy this file to .env and fill in the values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# LOCAL DEVELOPMENT OPTIONS:
# 
# Option 1: SQLite (zero config, good for quick dev)
DATABASE_URL=sqlite:///./dev.db

# Option 2: Docker PostgreSQL (more realistic, matches production)
# docker run -d --name postgres -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres:16
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/myapp_dev

# Option 3: Neon PostgreSQL (free tier, serverless)
# DATABASE_URL=postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# PRODUCTION (Neon):
# Set via Azure App Service configuration, not .env file
# DATABASE_URL=postgresql://neondb_owner:xxx@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Debug mode (set to false in production)
DEBUG=true

# Frontend URL for CORS (optional, uses wildcard if not set)
# FRONTEND_URL=https://your-app.azurestaticapps.net

# =============================================================================
# NOTES
# =============================================================================
# 
# For production deployment to Azure:
# 1. Do NOT commit .env file with secrets
# 2. Set DATABASE_URL via: az webapp config appsettings set --settings DATABASE_URL="..."
# 3. Neon free tier has cold starts (5-15s) - app handles this with timeouts
